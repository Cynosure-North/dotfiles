[ids]

0001:0001:09b4e68d
# *

[main]

media = f16

capslock = overload(caps_layer, esc)

[caps_layer]

leftshift = capslock
left = home
right = end
up = pageup
down = pagedown
e = command(sudo -u north nautilus)
f = command(sudo -u north WAYLAND_DISPLAY=wayland-0 firefox -P Personal)
w = command(sudo -u north WAYLAND_DISPLAY=wayland-0 firefox -P Work)
c = compose
# Can't figure out vscodium, probably because of electron weirdness

# The Run dialog (A-f2) is inconsistent

# NOTE when installing on new computer
# https://github.com/rvaiya/keyd?tab=readme-ov-file#why-is-my-trackpad-is-interfering-with-input-after-enabling-keyd
